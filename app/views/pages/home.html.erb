<% content_for :home do %>
  <% if user_signed_in? %>
  <div class="container">
    <br>
    <% if current_user.driver? %>
      <%= render "orders/orders" %>
    <% else  %>
  <%= link_to "Категорий", categories_path, class: 'btn btn-primary' %>
  <% end %>
  </div>
  <% else %>
  <div class="bg-overlay">
    <div class="text-center">
  		<h1>Социальная помощь пойска работы</h1>
      <br><br>
      <%= link_to "Найти объявление", add_order_path, class: 'btn btn-primary btn-lg' %>
    </div>
  </div>
  
<% end %>
<% end %>

